name: aime2024
output_dir: benchmark_results/1B-1.5B/aime/spec/

dataset:
  name: HuggingFaceH4/aime_2024
  split: train

enable_spec_diff: true

# Generator model configuration (lower memory for offloading)
generator_model:
  model: "meta-llama/Llama-3.2-1B-Instruct"

  gpu_memory_utilization: 0.45
  tensor_parallel_size: 1
  enable_prefix_caching: true
  seed: 42

# Verifier model configuration (lower memory for offloading)
verifier_model:
  model: "Skywork/Skywork-o1-Open-PRM-Qwen-2.5-1.5B"

  gpu_memory_utilization: 0.45
  tensor_parallel_size: 1
  enable_prefix_caching: true
  seed: 42

search_config:
  approach: beam_search
  beam_width: 4
  n: 64
  num_iterations: 10

  temperature: 0.8
