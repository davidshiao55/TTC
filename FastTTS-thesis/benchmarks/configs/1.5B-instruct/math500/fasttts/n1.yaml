name: math500
output_dir: benchmark_results/1.5B-instruct/math500/fasttts/

dataset:
  name: HuggingFaceH4/MATH-500
  split: test

enable_spec_diff: true
offload_enabled: false
prefix_aware_scheduling: true

# Generator
generator_model:
  model: "Qwen/Qwen2.5-1.5B-Instruct"
  max_model_len: 8192
  gpu_memory_utilization: 0.24
  tensor_parallel_size: 1
  enable_prefix_caching: true
  seed: 42

# Verifier (Skywork PRM, 1.5B, fixed)
verifier_model:
  model: "Skywork/Skywork-o1-Open-PRM-Qwen-2.5-1.5B"
  gpu_memory_utilization: 0.16
  tensor_parallel_size: 1
  enable_prefix_caching: true
  seed: 42

# N=1 reference point: no beam search exploration (single CoT chain).
# beam_width MUST equal n=1 to satisfy the n % beam_width == 0 constraint.
search_config:
  approach: beam_search
  beam_width: 1
  n: 1
  num_iterations: 10
  temperature: 0.8
