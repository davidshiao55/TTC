name: aime
output_dir: benchmark_results/7B-instruct/aime/baseline/

dataset:
  name: HuggingFaceH4/aime_2024
  split: train

enable_spec_diff: false
offload_enabled: false
prefix_aware_scheduling: false

# Generator
generator_model:
  model: "Qwen/Qwen2.5-7B-Instruct"
  max_model_len: 8192
  gpu_memory_utilization: 0.68
  tensor_parallel_size: 1
  enable_prefix_caching: true
  seed: 42

# Verifier (Skywork PRM, 1.5B, fixed)
verifier_model:
  model: "Skywork/Skywork-o1-Open-PRM-Qwen-2.5-1.5B"
  gpu_memory_utilization: 0.22
  tensor_parallel_size: 1
  enable_prefix_caching: true
  seed: 42

# N=1 reference point: no beam search exploration (single CoT chain).
# beam_width MUST equal n=1 to satisfy the n % beam_width == 0 constraint.
search_config:
  approach: beam_search
  beam_width: 1
  n: 1
  num_iterations: 10
  temperature: 0.8
