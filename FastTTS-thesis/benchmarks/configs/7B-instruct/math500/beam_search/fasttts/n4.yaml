name: math500
output_dir: benchmark_results/7B-instruct/math500/beam_search/fasttts/

dataset:
  name: HuggingFaceH4/MATH-500
  split: test

enable_spec_diff: true
offload_enabled: false
prefix_aware_scheduling: true

# Generator
generator_model:
  model: "Qwen/Qwen2.5-7B-Instruct"
  max_model_len: 8192
  gpu_memory_utilization: 0.74
  tensor_parallel_size: 1
  enable_prefix_caching: true
  seed: 42

# Verifier (Skywork PRM, 1.5B, fixed)
verifier_model:
  model: "Skywork/Skywork-o1-Open-PRM-Qwen-2.5-1.5B"
  gpu_memory_utilization: 0.16
  tensor_parallel_size: 1
  enable_prefix_caching: true
  seed: 42

search_config:
  approach: beam_search
  beam_width: 4
  n: 4
  num_iterations: 40
  temperature: 0.7
  spec_truncation_ratio: 0.0  # R=0 for true vanilla beam equivalence (paper §4.1.1)
